library(GAPR)
library(readr)

bbc <- read_delim("C:/Users/USER/Documents/GitHub/psychic-spoon/0 Meeting/cancer_no_y.csv", delim = "\t", col_names = TRUE, show_col_types = FALSE)

bbc <- as.data.frame(lapply(bbc, function(x) as.numeric(as.character(x))))

n_original <- rownames(bbc)
x_original <- colnames(bbc)

respond = GAP(
    data = bbc,
    row.prox = 'pearson',
    col.prox = 'pearson',
    row.order = 'average',
    col.order = 'average',
    border = TRUE,
    PNGfilename = 'cancer的GAP.png',
    show.plot = TRUE,
    exp.row_order = TRUE,
    exp.row_prox = TRUE
)

rowprox <- as.matrix(respond$row_prox)
doc_order <- respond$row_order
doc_numbers <- n_original[doc_order]
doc_order
# 分群：可視需要切成 K 群（例：2 群）
doc_cluster <- cutree(hclust(as.dist(1 - rowprox)), k = 2)

doc_numbers[doc_cluster == 1]
doc_numbers[doc_cluster == 2]
doc_numbers[doc_cluster == 3]
gap_cluster_df <- data.frame(filename = doc_numbers, gap_cluster = doc_cluster)
write.csv(gap_cluster_df, "gap_cancer_clusters.csv", row.names = FALSE)
cat("✅ GAP 分群結果已儲存為 gap_icancerclusters.csv\n")


